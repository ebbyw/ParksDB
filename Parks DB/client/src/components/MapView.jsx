// Reserved for future extraction. The map is currently rendered inline in
// App.jsx — once we add a "list view" mode, the map will move here so both
// pages can share the same Leaflet config.
export {};
